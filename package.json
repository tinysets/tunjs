{
  "name": "portmp",
  "version": "1.0.0",
  "description": "port mapping",
  "main": "index.js",
  "types": "./types/index.d.ts",
  "typings": "./types/index.d.ts",
  "scripts": {
    "test": "echo test ok! && exit 0",
    "build": "tsc",
    "build:w": "tsc -w",
    "build:types": "tsc -p tsconfig.types.json",
    "clean": "rimraf src/**/*.js src/**/*.js.map && rimraf index.js index.js.map cli.js cli.js.map test.js test.js.map"
  },
  "bin": {
    "portmp": "./cli.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^8.3.0",
    "delay": "^5.0.0",
    "koa-compose": "^4.1.0",
    "once": "^1.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.9",
    "rimraf": "^3.0.2",
    "typescript": "^5.2.2"
  }
}